/**
 * this is typedef1 desc.
 * @typedef {(number|string)} typedef1
 * @public
 * @deprecated
 * @since v1.2.3
 * @memberof Namespace1
 * @see http://example.com
 * @see {@link Namespace1.Class1}
 * @see {@link external:XMLHttpRequest}
 * @example
 * var foo = typedef1;
 */

/**
 * this is typedef2 desc.
 * @typedef {boolean} typedef2
 * @protected
 */

/**
 * this is typedef3 desc.
 * @private
 * @typedef {Object} typedef3
 * @property {number} [typedef3.member1=10] this is member1 desc.
 * @property {?string} typedef3.member2 this is member2 desc.
 * @property {Object} typedef3.member3 this is member3 desc.
 * @property {!boolean} typedef3.member3.member4 this is member4 desc.
 * @example
 * var foo = typedef3;
 */
