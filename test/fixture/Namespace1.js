/**
 * this is Namespace1 desc.
 * @namespace Namespace1
 * @private
 * @since 1.2.3
 * @see http://example.com
 */

/**
 * this is method1 desc.
 * @memberof Namespace1
 * @protected
 * @deprecated
 * @example
 * var foo = method1;
 * @param {external:XMLHttpRequest} p1 this is p1 desc.
 * @param {!string} p2 this is p2 desc.
 * @param {?string} p3 this is p3 desc.
 * @param {number} [p4=123] this is p3 desc.
 * @param {Object} p5
 * @param {string[]} p5.p6 this is p5.p6 desc.
 * @return {string} this is return desc.
 * @throw {Error} this is throw desc.
 * @since 1.2.3
 */
function method1(p1, p2, p3, p4, p5){}

/**
 * this is member1 desc.
 * @memberof Namespace1
 * @member {string}
 * @public
 * @deprecated
 * @readonly
 * @example
 * var foo = member1;
 * @since 1.2.3
 */
var member1;

/**
 * @namespace
 * @protected
 * @example
 * var foo = Namespace2;
 * @since 1.2.3
 */
var Namespace2;
